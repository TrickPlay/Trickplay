Index: fontconfig-2.8.0/src/fcxml.c
===================================================================
--- fontconfig-2.8.0.orig/src/fcxml.c
+++ fontconfig-2.8.0/src/fcxml.c
@@ -1123,6 +1123,7 @@ FcStrtod (char *s, char **end)
      * Have to swap the decimal point to match the current locale
      * if that locale doesn't use 0x2e
      */
+#ifndef __ANDROID__
     if ((dot = strchr (s, 0x2e)) &&
 	(locale_data = localeconv ()) &&
 	(locale_data->decimal_point[0] != 0x2e ||
@@ -1159,6 +1160,7 @@ FcStrtod (char *s, char **end)
 	}
     }
     else
+#endif
 	v = strtod (s, end);
     return v;
 }
