security {

	// see DefaultSecurityConfig.groovy for all settable/overridable properties

	active = true

	loginUserDomainClass = "trickplay.User"
	authorityDomainClass = "trickplay.Role"
	requestMapClass = "trickplay.Requestmap"
}
